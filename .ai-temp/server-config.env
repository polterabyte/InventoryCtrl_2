# Centralized Server Configuration
# Change this IP address to configure the entire system

# Server IP Configuration
SERVER_IP=192.168.139.96
SERVER_DOMAIN=warehouse.cuby
STAGING_DOMAIN=staging.warehouse.cuby
TEST_DOMAIN=test.warehouse.cuby

# API URLs
API_HTTP_URL=http://${SERVER_IP}/api
API_HTTPS_URL=https://${SERVER_IP}/api
SIGNALR_HTTP_URL=http://${SERVER_IP}/notificationHub
SIGNALR_HTTPS_URL=https://${SERVER_IP}/notificationHub
HEALTH_HTTP_URL=http://${SERVER_IP}/health
HEALTH_HTTPS_URL=https://${SERVER_IP}/health

# CORS Origins
CORS_ORIGINS=https://${SERVER_IP},http://${SERVER_IP},https://localhost:5001,http://localhost:5001,https://localhost:7001,http://localhost:7001,http://10.0.2.2:8080,capacitor://localhost
